"""
OpenRouter client — built out in Phase 4 (interview chat engine).
Left as a placeholder in Phase 0 so the file exists at the path the
spec expects (Section 3): backend/core/openrouter_client.py
"""

# Real implementation (chat completions against OpenRouter using
# openai/gpt-4o-mini) lands in Phase 4. Do not implement early.

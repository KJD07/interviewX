from django.apps import AppConfig


class CompaniesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.companies"
    # Company, Role, Round, InterviewQuestion models land here in Phase 1.
